/** Valores alineados a los CHECK constraints de la base Rimac21Instruccion */

export enum AccountStatus {
  ACTIVA = 'ACTIVA',
  BLOQUEADA = 'BLOQUEADA',
  INACTIVA = 'INACTIVA',
}

export enum CommonStatus {
  ACTIVO = 'ACTIVO',
  INACTIVO = 'INACTIVO',
}

export enum GroupStatus {
  ACTIVO = 'ACTIVO',
  CERRADO = 'CERRADO',
  CANCELADO = 'CANCELADO',
}

export enum MemberStatus {
  ACTIVO = 'ACTIVO',
  FINALIZADO = 'FINALIZADO',
  RETIRADO = 'RETIRADO',
  SUSPENDIDO = 'SUSPENDIDO',
}

export enum AssignmentStatus {
  ACTIVO = 'ACTIVO',
  FINALIZADO = 'FINALIZADO',
}

export enum SessionStatus {
  ACTIVA = 'ACTIVA',
  REVOCADA = 'REVOCADA',
  EXPIRADA = 'EXPIRADA',
}
